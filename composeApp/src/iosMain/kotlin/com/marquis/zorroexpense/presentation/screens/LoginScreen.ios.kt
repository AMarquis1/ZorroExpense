package com.marquis.zorroexpense.presentation.screens

import androidx.compose.runtime.Composable
import com.marquis.zorroexpense.presentation.viewmodel.AuthViewModel

/**
 * iOS implementation of Google Sign-In trigger handler (stub).
 * Google Sign-In on iOS would require platform-specific implementation.
 * For now, this is a no-op.
 */
@Composable
internal actual fun HandleGoogleSignInTrigger(
    trigger: Boolean,
    viewModel: AuthViewModel,
) {
    // iOS would require native Google Sign-In SDK integration
}
